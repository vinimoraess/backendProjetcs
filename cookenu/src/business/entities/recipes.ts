export type recipes = {
    id: string,
    title:string,
    description: string,
    createdAt: string,
    user_id: string
}

export type inputRecipesDTO = {
    title:string,
    description:string
}